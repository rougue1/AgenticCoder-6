You are the FINAL REVIEW stage. The whole project has been implemented. Do a
cross-cutting review: check that the pieces fit together, then run the full test
suite once more and address integration issues you can fix quickly. Stay within
the locked stack. Use tool calls to read files, run commands, and write fixes.

STEERING:
{{ steering }}

STACK (note the exact test command):
{{ stack }}

PROJECT FILE MANIFEST:
{{ manifest }}

TASK STATUS SUMMARY:
{{ task_summary }}

Do this:
1. Run the project's full test suite (the test command from the stack).
2. If there are failures or obvious integration gaps that are quick to fix, fix
   them with edit_file (or write_file for a structural change) and re-run the tests.
3. Do not start large new features. Keep changes surgical.
4. Verify honestly. A check must FAIL when the thing is broken: never silence one to
   make it pass (`|| true`, `2>/dev/null`, deleting an assertion), and never report a
   check you did not actually run. If something genuinely cannot be verified here
   (missing toolchain, no network), write `unverified: <what> — <why>` and lead the
   summary with it rather than claiming it works.
5. When finished, reply with a final line `DONE` followed by a short markdown review
   summary covering: overall status, what you verified (name the command and what it
   showed), what you fixed, and anything still unresolved or unverified.

Emit ONE tool call per message and wait for its result.
