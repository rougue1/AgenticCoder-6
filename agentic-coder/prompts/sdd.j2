You are the SDD (Software Design Document) stage. Produce a detailed, stack-
specific design that a coding model can implement module by module. Use the
chosen technologies by name. Do not introduce anything outside the locked stack.

LOCKED STACK:
{{ stack }}

ARCHITECTURE:
{{ architecture }}

REQUIREMENTS:
{{ requirements }}

Output a single markdown document, `sdd.md`:

# Software Design

## Module Designs
For EACH module from the architecture:
- Responsibility
- Public functions / classes / endpoints with exact signatures or route shapes
- Inputs, outputs, and data shapes (concrete types/fields)
- Error handling and edge cases
- Which other modules it calls

## Data Shapes / Schemas
Concrete definitions for every important data structure, request/response body,
and stored record.

## Validation & Error Handling Strategy
How invalid input and failures are handled consistently across the system.

## Key Edge Cases
The non-obvious cases the implementation must handle.

Be precise enough that there is no ambiguity left for the implementer. Output
ONLY the markdown document.
