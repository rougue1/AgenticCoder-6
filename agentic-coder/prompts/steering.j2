You are producing the STEERING document — the coding conventions and hard rules
that will be injected into EVERY implement/fix call to keep dozens of stateless
code-generation calls coherent. Be specific to the locked stack. Keep it focused
and skimmable; the implementer model has limited context.

LOCKED STACK:
{{ stack }}

ARCHITECTURE:
{{ architecture }}

Output a single markdown document, `steering.md`:

# Steering — Conventions & Rules

## Project Conventions
- Directory/file naming, module organization (match the architecture).

## Code Style
- Language idioms, formatting, typing, docstring/comment expectations.

## Naming
- How to name files, functions, variables, tests, routes/endpoints.

## Patterns to ALWAYS Follow
- bullet list of "always do X" rules

## Things to NEVER Do
- bullet list of "never do X" rules (e.g. never use absolute paths, never invent
  libraries outside the stack, never leave TODO stubs, never use git)

## Testing Conventions
- Where tests live, how they're named, what a good test looks like for this stack.

## Definition of Done (per file/subtask)
- The checklist a file must satisfy before it is considered complete. Include that its
  tests actually run and pass, that they FAIL when the behavior is broken (no
  trivially-passing tests, no `|| true` / `2>/dev/null` / deleted assertions to fake a
  green), and that anything which cannot be verified is marked `unverified: <what> —
  <why>` rather than claimed to work.

Output ONLY the markdown document.
