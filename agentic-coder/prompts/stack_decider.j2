You are the STACK DECISION stage. Choose ONE concrete, coherent technology stack
that best fits the requirements. This decision LOCKS the stack — every later
stage must use exactly these technologies and nothing else, so be decisive and
specific.

PROJECT BRIEF:
{{ project_brief }}

REQUIREMENTS:
{{ requirements }}

Prefer mainstream, well-supported choices that are easy to run and test locally
with minimal setup. Avoid anything requiring paid services, Docker, or network
access beyond standard package installs. Pin major versions where it matters.

Output a single markdown document, `stack.md`:

# Technology Stack

## Summary
One paragraph: the stack in a sentence and why it fits these requirements.

## Languages & Runtimes
- e.g. Python 3.11, Node.js 20

## Frameworks & Key Libraries
- name — purpose (one line each)

## Data Storage
- the database/storage and why (or "none / in-memory" if truly not needed)

## Testing
- test runner and assertion style

## Package Manager
- the exact package manager

## Commands (EXACT, copy-pasteable, run from the project root)
- install: `<command to install all dependencies>`
- test: `<command to run the full test suite>`
- run: `<command to start the app>`
- lint (optional): `<command or "none">`

Every command must be runnable as-is on a clean machine after install. Output
ONLY the markdown document.
