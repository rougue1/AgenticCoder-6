The verification FAILED. Fix the problem using tool calls — one per message.
Make the smallest correct change that addresses the actual error; do not
rewrite unrelated code.

Test command: {{ cmd }}
Exit code: {{ exit_code }}
Attempt: {{ attempt }} of {{ max_attempts }}

--- STDOUT ---
{{ stdout if stdout else "(empty)" }}

--- STDERR ---
{{ stderr if stderr else "(empty)" }}

Diagnose the root cause, then:
1. Use read_file if you need to re-check the current contents of a file.
2. Use patch_file to make the smallest targeted change that fixes the actual
   error (replace the exact broken lines). Only fall back to write_file with
   the COMPLETE contents when the fix is structural and touches most of the
   file.
3. Never silence a failure to go green — no `|| true`, no deleted assertions.
4. When your fix is in place, reply with the single line: DONE

The orchestrator re-runs the verification after you say DONE.
