You are the REQUIREMENTS step. Derive complete, testable requirements from the
project brief, staying inside the locked stack from your system context.

PROJECT BRIEF:
{{ project_brief }}

Output a single markdown document, `requirements.md`, with these sections:

# Requirements

## Functional Requirements
Numbered list (FR-1, FR-2, ...). Each is a single, testable capability.

## Non-Functional Requirements
Numbered list (NFR-1, ...): performance, reliability, security, usability —
only those that genuinely apply.

## Core Entities / Data Model
The key domain objects with their important fields and relationships.

## User Flows
The primary end-to-end flows, step by step.

## Assumptions & Open Questions
Anything you had to assume; note the sensible defaults you chose.

Be thorough but do not pad. Output ONLY the markdown document.
