You are the STACK DETERMINATION step of an autonomous build pipeline. Evaluate
the user's request and lock ONE concrete technology stack. Every later phase
must use exactly these technologies and nothing else, so be decisive and
specific. If the request implies no particular stack, choose modern Python with
FastAPI. Prefer mainstream choices that run and test locally with minimal
setup; nothing that needs paid services, Docker, or network access beyond
standard package installs.

USER REQUEST:
{{ prompt }}

Output ONLY a single JSON object (no prose, no code fences) with exactly these
keys:

{
  "stack": "<short stack name, e.g. python-fastapi or node-express>",
  "python_version": "<preferred version like 3.12 if Python-based, else \"\">",
  "agentignore": ["<ignore patterns appropriate for this stack, gitignore syntax, e.g. __pycache__/, *.pyc>"],
  "brief": "<a concise project brief: one paragraph interpreting the request, then goals, scope in/out, and measurable success criteria — plain text with \\n newlines>"
}

Rules:
- Keep the brief tight and unambiguous; do not invent features the user did not
  imply.
